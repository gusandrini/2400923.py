#calcular a soma de uma lista de números (com recursão)

def soma_numeros(lista):
    if(len) == 1
        return lista[0]
    else:
        return lista[0] + soma_numeros(lista[1:])

#principal 
lista = [1, 3, 5, 7, 9]
pritn(f'{soma_numeros(lista)}')